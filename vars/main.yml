---
# vars file for bareos_sd

bareos_sd_packages:
  - bareos-storage
  - bareos-storage-tape

_bareos_sd_debug_packages:
  default:
    - "bareos-storage-debuginfo"
  Debian:
    - "bareos-storage-dbg"
bareos_sd_debug_packages: "{{ _bareos_sd_debug_packages[ansible_os_family] | default(_bareos_sd_debug_packages['default']) }}"
